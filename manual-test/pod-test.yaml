apiVersion: v1
kind: Pod
metadata:
  name: net-test
  namespace: app
spec:
  serviceAccountName: order-service
  containers:
  - name: test
    image: alpine:3.20
    command:
    - sh
    - -c
    - |
      apk add --no-cache busybox-extras
      sleep 3600