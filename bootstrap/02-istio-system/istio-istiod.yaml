pilot:
  env:
    PILOT_ENABLE_AMBIENT: "true"
    PILOT_ENABLE_AMBIENT_WAYPOINTS: "true"


# Khai báo để chặn traffic đi ra ngoài external nếu chưa khai báo ServiceEntry
meshConfig:
  outboundTrafficPolicy:
    mode: REGISTRY_ONLY