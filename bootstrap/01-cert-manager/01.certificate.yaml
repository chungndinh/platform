apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: chungndinh-com-wildcard
  namespace: istio-ingress
spec:
  secretName: chungndinh-com-wildcard-tls

  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer

  dnsNames:
    - chungndinh.com
    - "*.chungndinh.com"