apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-api-gateway-to-api-order-service
  namespace: app
spec:
  selector:
    matchLabels:
      app: order-service

  action: ALLOW

  rules:
  - from:
    - source:
        principals:
        - cluster.local/ns/middle/sa/api-gateway